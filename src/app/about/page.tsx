import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'About USC',
  description: 'About United Systems Cooperative',
};

const AboutPage = () => {
  return <></>;
};

export default AboutPage;
