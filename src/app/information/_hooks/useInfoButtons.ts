import { uscLinksList } from '../../about/uscLinksList';
import { docsList, guidesList, odysseyList, toolsList } from '../_data';

export const useInfoButtons = () => {
  return { docsList, guidesList, toolsList, odysseyList, uscLinksList };
};
